import CreateProfileCard from "@/components/login/create-profile-card";
import LoginCard from "@/components/login/login-card";

const CreateProfile = () => {
  return (
    <LoginCard>
      <CreateProfileCard />
    </LoginCard>
  );
};

export default CreateProfile;
